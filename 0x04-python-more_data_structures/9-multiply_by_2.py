#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    b_dictionary = a_dictionary.copy()
    list_key = list(b_dictionary.key())

    for x in list_key
        b_dictionary[x] *= 2
        return b_dictionary
